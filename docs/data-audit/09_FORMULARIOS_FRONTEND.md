# Formulários, Tabelas, Filtros e Dashboards

A varredura localizou 25 candidatos de campo por atributos `name`/`id`; isso não comprova binding. Cada linha deve ser vinculada a DTO, endpoint, permissão, validação, auditoria e teste.

EVIDÊNCIAS: `artifacts/matriz_formulario_campo.csv`. Lacuna: `AUD-P1-002`.
