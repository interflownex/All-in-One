# APIs, Eventos e Integrações

A varredura sintática localizou 99 endpoints candidatos. A matriz ainda requer DTOs, campos, scopes, idempotência, erros, eventos, consumidores e testes de contrato.

EVIDÊNCIAS: `artifacts/matriz_api_campo.csv` e `artifacts/matriz_evento_campo.csv`. Lacuna: `AUD-P1-003`.
