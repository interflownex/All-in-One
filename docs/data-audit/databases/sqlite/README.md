# sqlite

Inventário estático de 4 tabelas e 53 campos do fallback local; instâncias não comprovadas. EVIDÊNCIAS: `artifacts/catalogo_sqlite.json`.

**Status:** catálogo parcial; lacuna `AUD-P0-000`.
