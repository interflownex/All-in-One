{
  "name": "@all-in-one/public-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/validate.mjs && node scripts/security.test.mjs",
    "dev": "npm run build && wrangler pages dev dist"
  },
  "devDependencies": {
    "wrangler": "4.123.0"
  }
}
