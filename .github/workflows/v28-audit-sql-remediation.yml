name: V2.8 Audit SQL Remediation (Archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - name: Archived remediation notice
        run: |
          echo "A correção da coluna authorization foi aplicada e validada no ciclo v2.8."
          echo "Esta workflow não altera arquivos nem cria commits."
