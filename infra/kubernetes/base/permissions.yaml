---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: permissions
  namespace: all-in-one
spec:
  replicas: 2
  selector:
    matchLabels: { app: permissions }
  template:
    metadata:
      labels: { app: permissions }
    spec:
      containers:
        - name: permissions
          image: us-central1-docker.pkg.dev/all-in-one-498012/all-in-one-repo/permissions:latest
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8000 }]
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
          envFrom:
            - configMapRef: { name: platform-config }
            - secretRef: { name: platform-secrets-placeholder }
---
apiVersion: v1
kind: Service
metadata:
  name: permissions
  namespace: all-in-one
spec:
  selector: { app: permissions }
  ports: [{ port: 80, targetPort: 8000 }]
