---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery
  namespace: all-in-one
spec:
  replicas: 2
  selector:
    matchLabels: {app: delivery}
  template:
    metadata:
      labels: {app: delivery}
    spec:
      containers:
        - name: delivery
          image: us-central1-docker.pkg.dev/all-in-one-498012/all-in-one-repo/delivery:latest
          ports: [{containerPort: 8000}]
          readinessProbe: {httpGet: {path: /health, port: 8000}}
          envFrom:
            - configMapRef: {name: platform-config}
            - secretRef: {name: platform-secrets-placeholder}
---
apiVersion: v1
kind: Service
metadata:
  name: delivery
  namespace: all-in-one
spec:
  selector: {app: delivery}
  ports: [{port: 80, targetPort: 8000}]
