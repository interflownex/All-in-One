---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-core
  namespace: all-in-one
spec:
  replicas: 2
  selector:
    matchLabels: {app: ai-core}
  template:
    metadata:
      labels: {app: ai-core}
    spec:
      containers:
        - name: ai-core
          image: us-central1-docker.pkg.dev/all-in-one-498012/all-in-one-repo/ai-core:latest
          ports: [{containerPort: 8000}]
          readinessProbe: {httpGet: {path: /health, port: 8000}}
          envFrom:
            - configMapRef: {name: platform-config}
            - secretRef: {name: platform-secrets-placeholder}
---
apiVersion: v1
kind: Service
metadata:
  name: ai-core
  namespace: all-in-one
spec:
  selector: {app: ai-core}
  ports: [{port: 80, targetPort: 8000}]
