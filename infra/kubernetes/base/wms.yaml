---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wms
  namespace: all-in-one
spec:
  replicas: 2
  selector:
    matchLabels: {app: wms}
  template:
    metadata:
      labels: {app: wms}
    spec:
      containers:
        - name: wms
          image: us-central1-docker.pkg.dev/all-in-one-498012/all-in-one-repo/wms:latest
          ports: [{containerPort: 8000}]
          readinessProbe: {httpGet: {path: /health, port: 8000}}
          envFrom:
            - configMapRef: {name: platform-config}
            - secretRef: {name: platform-secrets-placeholder}
---
apiVersion: v1
kind: Service
metadata:
  name: wms
  namespace: all-in-one
spec:
  selector: {app: wms}
  ports: [{port: 80, targetPort: 8000}]
